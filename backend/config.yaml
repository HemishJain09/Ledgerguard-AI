# Ledger Guard Configuration

ingestion:
  math_tolerance_amount: 0.10

reconciliation:
  target_currency: "USD"
  max_settlement_lag_days: 3
  fuzzy_match_threshold: 75
  max_fee_pct: 0.03

solver:
  timeout_seconds: 1.5
  integer_scale_factor: 10000
  max_cluster_size: 500
